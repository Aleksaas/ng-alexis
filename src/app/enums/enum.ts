export enum Events {
    SHIP_CREATED,
    SHIP_UPDATED,
    SHIP_TRANSFERED,

    CUSTOMER_CHANGED,
    CUSTOMER_CREATED,
}

export enum SortOrder {
    ASC,
    DESC
}

export enum Severity {
    ERROR = 1,
    WARNING = 2
}

export const Roles = {
    ADMIN: 'admin'
}
